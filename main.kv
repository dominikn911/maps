#:include restaurantmapview.kv

BoxLayout:
    orientation: 'vertical'
    MDToolbar:
        title: "Find your Restaurant or Pub"
        right_action_items: [['magnify', lambda x: app.search_menu.open()]]
        md_bg_color: app.theme_cls.primary_color
    RestaurantMapView:
        id: mapview